<article class="card card--rounded container--flex-column">
    <div class="container--flex-column card--description">
        <a class="card--title" href="{{ path('app_category_show', {id: categoryId}) }}">{{ category }}</a>
    </div>
</article>