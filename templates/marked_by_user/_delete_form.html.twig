<form method="post" action="{{ path('app_marked_by_user_delete', {'id': marked_by_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marked_by_user.id) }}">
    <button class="btn">Delete</button>
</form>
