{% extends 'base.html.twig' %}

{% block title %}MarkedByUser index{% endblock %}

{% block body %}
    <h1>MarkedByUser index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for marked_by_user in marked_by_users %}
            <tr>
                <td>{{ marked_by_user.id }}</td>
                <td>
                    <a href="{{ path('app_marked_by_user_show', {'id': marked_by_user.id}) }}">show</a>
                    <a href="{{ path('app_marked_by_user_edit', {'id': marked_by_user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_marked_by_user_new') }}">Create new</a>
{% endblock %}
