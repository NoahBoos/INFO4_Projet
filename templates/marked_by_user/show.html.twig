{% extends 'base.html.twig' %}

{% block title %}MarkedByUser{% endblock %}

{% block body %}
    <h1>MarkedByUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marked_by_user.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marked_by_user_index') }}">back to list</a>

    <a href="{{ path('app_marked_by_user_edit', {'id': marked_by_user.id}) }}">edit</a>

    {{ include('marked_by_user/_delete_form.html.twig') }}
{% endblock %}
